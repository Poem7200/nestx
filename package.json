{
  "name": "nestx-monorepo",
  "private": true,
  "workspaces": [
    "docs/*",
    "app/*",
    "packages/*"
  ],
  "scripts": {
    "docs:dev": "pnpm --filter docs docs:dev",
    "docs:build": "pnpm --filter docs docs:build"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "inquirer": "^9.2.7",
    "typescript": "^5.2.2"
  }
}
